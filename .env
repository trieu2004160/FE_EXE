# Environment variables for NOVA e-commerce platform
# .NET backend integration
# Use HTTP for frontend (port 8081) to backend (port 5000) communication
VITE_API_BASE_URL=http://localhost:5000/api

# Frontend HTTPS configuration (currently disabled due to certificate issues)
HTTPS=false
